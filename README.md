
  # Sales Order Workflow App

  This is a code bundle for Sales Order Workflow App. The original project is available at https://www.figma.com/design/zStEIOPLs1mKuH9ruapvlb/Sales-Order-Workflow-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  